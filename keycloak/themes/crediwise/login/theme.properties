# CrediWise login theme
# Inherits all pages (error, reset-password, etc.) from the built-in
# "keycloak" theme.  Only login.ftl and its CSS are overridden here.
parent=keycloak
import=common/keycloak
