# Copy to .env and fill in values for production
# Dev values are hardcoded in application.properties — do not use this in dev

DB_USERNAME=admin
DB_PASSWORD=changeme_in_production
GESTIONNAIRE_DB_URL=jdbc:postgresql://localhost:5432/gestionnaire_db

# JWT — generate with: openssl genrsa -out privateKey.pem 2048
# Place privateKey.pem and publicKey.pem in this directory
