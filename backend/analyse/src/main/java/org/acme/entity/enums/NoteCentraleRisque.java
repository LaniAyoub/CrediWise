package org.acme.entity.enums;

/**
 * Credit risk rating from the central risk office (Centrale des Risques).
 * A = best, E = worst — Step 3 (Risque Client) Section 3.
 */
public enum NoteCentraleRisque {
    A, B, C, D, E
}
